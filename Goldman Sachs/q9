 string printMinNumberForPattern(string s){
        string t = "";
stack<int> st;
for(int i=0;i<=s.length();i++){ //IIDDD
st.push(i+1);
if(s.length()==i || s[i]=='I' ){
while(!st.empty()){
t += to_string(st.top());
st.pop();
}
}
}
return t;
}
